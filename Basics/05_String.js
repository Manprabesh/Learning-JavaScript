const name="Boruah"
const college="DR college"
const program="BCA";

console.log(`My name is ${name} and I am studying in ${college} college with a ${program} degree`)

// console.log(name[0])

const gameName=new String("Gambler");

console.log(gameName.indexOf('a'))//return the index of the string
console.log(gameName.slice(-7,-1))
//slice(indexStart)
// slice(indexStart, indexEnd)